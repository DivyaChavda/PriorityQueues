package com.aplshd.priorityqueues;

/**
 * Implements the Priority Queue using
 * Binary Heap data structure
 */
public class FibonacciHeap {
	
	/**
	 * Creates a new heap containing no elements 
	 */
	public void makeHeap() {
		
	}
	
	/**
	 * inserts the element
	 * @param key
	 */
	void insert(int key) {
		
	}
	
	/**
	 * deletes the element
	 * @param key
	 */
	void delete(int key) {
		
	}
	
	/**
	 * deletes the node from the heap whose
	 * key is minimum, returning a pointer to the node
	 * @return
	 */
	int extractMin() { //To - Do change return type
		return 0;
		
	}
	
	/**
	 * returns a pointer to the node whose key is minimum
	 * @return
	 */
	int findMin() { //To - Do change return type
		return 0;
		
	}
	
	/**
	 * increases the key of the node
	 * @param key
	 */
	void increase(int key) {
		
	}
	
	/**
	 * decreases the key of the node
	 * @param key
	 */
	void decrease(int key) {
		
	}
	
	/**
	 * Tree structure of the priority queue is
	 * stored in the fileName in dot language format
	 * @param fileName
	 */
	void displayHeap(String fileName) {
		
	}
}